Backend Task API

Setup
1. npm install
2. create .env with MONGODB_URI and PORT
3. npm start

Endpoints
- GET /api/tasks
- GET /api/tasks/:id
- POST /api/tasks
- PUT /api/tasks/:id
- DELETE /api/tasks/:id
