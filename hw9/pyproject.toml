[project]
name = "hw9-demo"
version = "0.1.0"
description = "Demo project for Redis caching"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "openai",
    "redis",
    "requests",
    "sentence-transformers",
]

[tool.uv]
package = true
